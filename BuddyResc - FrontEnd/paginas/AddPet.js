import React, { useState } from "react";
import { Alert, StyleSheet, Text, Image, TouchableOpacity, View, SafeAreaView, ScrollView } from "react-native";


export default function AdicionarPets({navigation}) {
    return(

    );
}